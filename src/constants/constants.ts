export const CLIENT_ID_SPOTIFY = String(
  process.env.NEXT_PUBLIC_CLIENT_ID_SPOTIFY
);

export const CLIENT_SECRET_SPOTIFY = String(
  process.env.NEXT_PUBLIC_CLIENT_SECRET_SPOTIFY
);

export const URL_API_TOKEN_SPOTIFY = String(
  process.env.NEXT_PUBLIC_URL_API_TOKEN_SPOTIFY
);

export const URL_API_SPOTIFY = String(process.env.NEXT_PUBLIC_URL_API_SPOTIFY);

export const URL_API_DEEZER = String(process.env.NEXT_PUBLIC_URL_API_DEEZER);
